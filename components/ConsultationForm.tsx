'use client'; import {useState} from 'react';
export default function ConsultationForm(){const [state,setState]=useState('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('loading');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/consultations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setState(r.ok?'success':'error');if(r.ok)e.currentTarget.reset()}if(state==='success')return <div className="card" style={{padding:40,textAlign:'center'}}><h3>Consultation request received.</h3><p className="muted">We'll confirm the requested time shortly.</p></div>;return <form className="card" onSubmit={submit}><div className="formgrid"><div><label>Name</label><input name="name" required/></div><div><label>Email</label><input name="email" type="email" required/></div><div><label>Phone / WhatsApp</label><input name="phone"/></div><div><label>Service</label><select name="service" required><option>AIOps Solutions</option><option>DevOps Services</option><option>Cloud & Infrastructure</option><option>Monitoring & Observability</option><option>Automation</option><option>AI Automation</option></select></div><div><label>Preferred Date</label><input name="date" type="date" required/></div><div><label>Preferred Time</label><input name="time" type="time" required/></div></div><div style={{marginTop:18}}><label>Notes</label><textarea name="notes" rows={5}/></div>{state==='error'&&<p style={{color:'#ff8d8d'}}>Unable to book right now.</p>}<button className="btn primary" style={{border:0,marginTop:22,cursor:'pointer'}}>{state==='loading'?'Booking…':'Book Consultation'}</button></form>}
