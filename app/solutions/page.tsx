import Link from 'next/link'; import Section from '@/components/Section';
const items=[['My infrastructure keeps going down.','Monitoring + alerting + proactive operations'],['Our deployments take too much time.','CI/CD + deployment automation'],["We don't know what's happening inside our infrastructure.",'Observability + dashboards + logging'],['Our team wastes time on repetitive tasks.','Automation + AI workflows'],['We want to move to the cloud.','Cloud infrastructure + migration planning']];
export default function Solutions(){return <Section eyebrow="Solutions" title="Start with the problem. Build the right solution."><div style={{display:'grid',gap:16}}>{items.map(([p,s])=><div className="card" key={p}><div style={{display:'grid',gridTemplateColumns:'1.1fr 1fr',gap:20,alignItems:'center'}}><h2 style={{margin:0}}>{p}</h2><div><div style={{color:'var(--accent)',fontWeight:800}}>NEXAOPS APPROACH</div><p className="muted">→ {s}</p></div></div></div>)}</div><div style={{marginTop:30}}><Link className="btn primary" href="/consultation">Discuss Your Project</Link></div></Section>}
