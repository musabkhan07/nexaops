'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok)router.push('/admin');else setError('Invalid email or password.')}return <main className="grid-bg" style={{minHeight:'75vh',display:'grid',placeItems:'center'}}><form className="card" onSubmit={submit} style={{width:'min(430px,92%)'}}><div style={{color:'var(--accent)',fontWeight:800,fontSize:12,letterSpacing:2}}>NEXAOPS ADMIN</div><h1>Sign in</h1><label>Email</label><input name="email" type="email" required/><div style={{height:14}}/><label>Password</label><input name="password" type="password" required/>{error&&<p style={{color:'#ff8d8d'}}>{error}</p>}<button className="btn primary" style={{border:0,cursor:'pointer',marginTop:20}}>Sign In</button></form></main>}
