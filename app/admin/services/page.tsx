import {prisma} from '@/lib/prisma';
export default async function Services(){const rows=await prisma.service.findMany({orderBy:{createdAt:'desc'}});return <main className="container" style={{padding:'50px 0'}}><h1>Services</h1><p className="muted">Database-backed service management is ready. Add CRUD forms here as the catalog grows.</p><div style={{display:'grid',gridTemplateColumns:'repeat(2,1fr)',gap:15}}>{rows.map(r=><div className="card" key={r.id}><h3>{r.name}</h3><p className="muted">{r.shortDescription}</p></div>)}{!rows.length&&<div className="card">No custom services yet. The public service catalog is currently code-defined.</div>}</div></main>}
