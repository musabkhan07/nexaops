import {prisma} from '@/lib/prisma';
export default async function Leads(){const rows=await prisma.lead.findMany({orderBy:{createdAt:'desc'},take:100});return <main className="container" style={{padding:'50px 0'}}><h1>Leads</h1><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr>{['Name','Email','Company','Service','Status','Date'].map(x=><th key={x} style={{textAlign:'left',padding:12,borderBottom:'1px solid var(--line)'}}>{x}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id}>{[r.name,r.email,r.company||'—',r.service,r.status,new Date(r.createdAt).toLocaleDateString()].map((x,i)=><td key={i} style={{padding:12,borderBottom:'1px solid var(--line)',color:i===4?'var(--accent)':'#b7c8da'}}>{x}</td>)}</tr>)}</tbody></table>{!rows.length&&<p className="muted">No leads yet.</p>}</div></main>}
