import {prisma} from '@/lib/prisma'; import Link from 'next/link';
export default async function Admin(){const [leads,consultations,messages,posts,cases]=await Promise.all([prisma.lead.count(),prisma.consultation.count(),prisma.contactMessage.count(),prisma.blogPost.count({where:{published:true}}),prisma.caseStudy.count({where:{published:true}})]);const cards=[['Total leads',leads],['Consultations',consultations],['Messages',messages],['Published posts',posts],['Case studies',cases]];return <main className="container" style={{padding:'55px 0'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div style={{color:'var(--accent)',fontWeight:800,fontSize:12}}>ADMIN DASHBOARD</div><h1>NexaOps Control Center</h1></div><form action="/api/auth/logout" method="post"><button className="btn secondary">Logout</button></form></div><div className="responsive-four" style={{display:'grid',gridTemplateColumns:'repeat(5,1fr)',gap:12,margin:'30px 0'}}>{cards.map(([x,n])=><div className="card" key={x as string}><div className="muted">{x as string}</div><div style={{fontSize:32,fontWeight:800}}>{n as number}</div></div>)}</div><div style={{display:'flex',gap:12,flexWrap:'wrap'}}>{['leads','consultations','services','blog','case-studies'].map(x=><Link className="btn secondary" href={'/admin/'+x} key={x}>Manage {x}</Link>)}</div></main>}
