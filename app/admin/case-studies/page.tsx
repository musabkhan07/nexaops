import {prisma} from '@/lib/prisma';
export default async function Cases(){const rows=await prisma.caseStudy.findMany({orderBy:{createdAt:'desc'}});return <main className="container" style={{padding:'50px 0'}}><h1>Case Studies</h1><p className="muted">Publish only real projects. The schema supports problem, solution, technologies, results, images and publication status.</p>{rows.map(r=><div className="card" style={{marginTop:12}} key={r.id}><h3>{r.projectName}</h3><span className="muted">{r.published?'Published':'Draft'}</span></div>)}{!rows.length&&<div className="card">Projects & Demonstrations Coming Soon.</div>}</main>}
